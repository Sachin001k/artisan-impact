// The Razorpay Key ID is your PUBLIC key — safe in client-side code.
// Get it from: Razorpay Dashboard → Settings → API Keys
// Never put the Key SECRET here — that only ever lives in Vercel env vars
// and is used inside api/create-order.js and api/verify-payment.js.
export const RAZORPAY_KEY_ID = 'rzp_test_REPLACE_ME'
